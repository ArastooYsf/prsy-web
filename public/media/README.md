Local placeholder for product images/videos, served at `NEXT_PUBLIC_MEDIA_URL`
(currently `/media`, i.e. this folder). See the project root `README.md` for
how to point the whole site at an external media host instead.
