# فعال‌سازی بخش سئو/گوگل/بک‌لینک در داشبورد لاگ

این چک‌لیست مربوط به کارت placeholder فعلی در داشبورد ادمین (`/account/admin/logs`) است — همان کارتی که فعلاً فقط متن «نیاز به اتصال به Google Search Console و PageSpeed API — بعد از آنلاین شدن سایت تنظیم می‌شود» را نشان می‌دهد. وقتی سایت روی یک دامنه‌ی واقعی و پابلیک آنلاین شد، مراحل زیر باید طی بشه تا این بخش با داده‌ی واقعی جایگزین بشه.

## ۱. Google Search Console

- سایت را در [Search Console](https://search.google.com/search-console) با دامنه‌ی نهایی ثبت کن (روش Domain property با رکورد TXT در DNS ترجیح داده می‌شود چون هر ساب‌دامین را هم پوشش می‌دهد).
- Sitemap را ثبت کن (`sitemap.xml` — اگر پروژه از قبل تولید نمی‌کند، باید اضافه شود).
- یک Service Account در [Google Cloud Console](https://console.cloud.google.com/) بساز، **Search Console API** را برایش فعال کن، و ایمیل Service Account را در Search Console به‌عنوان کاربر (نقش Restricted کافیست) اضافه کن.
- کلید JSON دانلودشده را به‌صورت متغیر محیطی امن (نه در ریپو) ذخیره کن — مثلاً `GOOGLE_SEARCH_CONSOLE_CREDENTIALS_JSON` یا مسیر فایل.
- داده‌هایی که از این API قابل‌دریافت‌اند: تعداد کلیک/ایمپرشن/CTR/میانگین رتبه، صفحات ایندکس‌شده، خطاهای پوشش (coverage errors)، Core Web Vitals گزارش‌شده توسط گوگل.

## ۲. Google PageSpeed Insights API

- یک API Key از [Google Cloud Console](https://console.cloud.google.com/apis/credentials) بگیر و **PageSpeed Insights API** را برایش فعال کن.
- محدودیت را روی همان کلید ست کن (IP restriction یا HTTP referrer restriction) تا کلید در صورت افشا سوءاستفاده نشود.
- به‌عنوان متغیر محیطی ذخیره شود — مثلاً `PAGESPEED_API_KEY`.
- این API یک جایگزین/مکمل برای تست Lighthouse محلی است که همین الان در داشبورد فعال است؛ تفاوتش این‌ست که روی داده‌ی واقعی کاربران (CrUX) هم گزارش می‌دهد، نه فقط یک اجرای آزمایشگاهی محلی.

## ۳. داده‌ی بک‌لینک (اختیاری)

اگر نیاز به رصد بک‌لینک/دامنه‌های ارجاع‌دهنده باشد، یکی از گزینه‌های زیر لازم است (فعلاً هیچ‌کدام انتخاب نشده‌اند):

- **Ahrefs** یا **Moz** — دقیق‌تر و معتبرتر، اما مشترک ماهانه پولی و نسبتاً گران دارند؛ هر دو API دارند.
- معادل رایگان‌تر/ایرانی — در زمان نگارش این یادداشت گزینه‌ی مشخصی بررسی و تأیید نشده؛ باید قبل از انتخاب، پوشش داده برای دامنه‌های `.ir` و کیفیت API را بررسی کرد.
- اگر بودجه/اولویت محدود است، این بخش را می‌توان در مرحله‌ی اول کلاً حذف کرد و فقط به داده‌ی Search Console + PageSpeed بسنده کرد.

## ۴. یادآوری‌های فنی هنگام پیاده‌سازی

- طبق همان الگوی فعلی پروژه، فراخوانی این APIها باید سمت سرور (Server Component یا API route) انجام شود، نه سمت کلاینت — کلیدها هرگز نباید به باندل کلاینت درز کنند.
- نتایج را مثل الگوی `getLighthouseHistory`/`runLighthouseAudit` (`src/lib/lighthouse.ts`) کش/ذخیره‌ی تاریخچه کن تا روند زمانی هم قابل نمایش باشد، نه فقط آخرین مقدار.
- بعد از پیاده‌سازی، کارت placeholder فعلی در `LogsDashboard.tsx` باید با یک `SeoStatsCard` واقعی (به همراه نمودار روند مشابه سایر کارت‌ها) جایگزین شود.
