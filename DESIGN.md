---
name: پویش راه صنعت یاشار
description: Dark industrial B2B control-room interface for a diesel generator and power-equipment supplier.
colors:
  background: "rgb(6 10 23)"
  foreground: "rgb(255 255 255)"
  signal-orange: "rgb(249 115 22)"
  signal-orange-on: "rgb(6 10 23)"
  identification-blue: "#3b82f6"
  card-surface: "rgb(15 23 42)"
  hairline: "rgba(255 255 255 / 0.10)"
  hairline-faint: "rgba(255 255 255 / 0.05)"
  surface-wash: "rgba(255 255 255 / 0.03)"
  muted-foreground: "rgb(148 163 184)"
  destructive: "rgb(239 68 68)"
  destructive-on: "rgb(6 10 23)"
typography:
  display:
    fontFamily: "Vazirmatn, sans-serif"
    fontSize: "clamp(1.875rem, 4vw, 3rem)"
    fontWeight: 700
    lineHeight: 1.2
    letterSpacing: "-0.01em"
  headline:
    fontFamily: "Vazirmatn, sans-serif"
    fontSize: "clamp(1.5rem, 3vw, 2.25rem)"
    fontWeight: 700
    lineHeight: 1.25
  body:
    fontFamily: "Vazirmatn, sans-serif"
    fontSize: "0.875rem"
    fontWeight: 400
    lineHeight: 1.6
  label:
    fontFamily: "Vazirmatn, sans-serif"
    fontSize: "0.6875rem"
    fontWeight: 600
    letterSpacing: "0.02em"
rounded:
  pill: "9999px"
  card: "1rem"
  input: "0.5rem"
  header: "1.5rem"
spacing:
  xs: "0.5rem"
  sm: "1rem"
  md: "1.5rem"
  lg: "2.5rem"
  section: "5rem"
components:
  button-primary:
    backgroundColor: "{colors.signal-orange}"
    textColor: "{colors.signal-orange-on}"
    rounded: "{rounded.input}"
    padding: "0.5rem 1rem"
  button-primary-hover:
    backgroundColor: "{colors.signal-orange}"
  button-outline:
    backgroundColor: "transparent"
    textColor: "{colors.foreground}"
    rounded: "{rounded.input}"
  card:
    backgroundColor: "{colors.surface-wash}"
    textColor: "{colors.foreground}"
    rounded: "{rounded.card}"
  input:
    backgroundColor: "rgba(255 255 255 / 0.05)"
    textColor: "{colors.foreground}"
    rounded: "{rounded.input}"
    padding: "0.75rem 1rem"
---

# Design System: پویش راه صنعت یاشار

## Overview

**Creative North Star: "اتاق کنترل" (The Control Room)**

سایت باید حس یک اتاق کنترل تجهیزات صنعتی رو بده: تیره، بی‌سروصدا، خوانا، و کاملاً کاربردی. هیچ سطحی برای خودش جلب توجه نمی‌کنه — پس‌زمینه‌ی نزدیک به مشکی و کارت‌های بی‌رنگ فقط بومی هستن که اطلاعات روشون قرار می‌گیره. تنها رنگی که اجازه‌ی جلب توجه داره نارنجی سیگنال است: دقیقاً مثل چراغ هشدار یا دکمه‌ی توقف اضطراری روی یک تابلوی برق — همیشه در حداقل مساحت ممکن، همیشه برای یک اقدام مشخص (CTA اصلی، وضعیت فعال، تمرکز روی فیلد).

مخاطب این سایت مسن‌تر، کم‌حوصله، و صنعتی‌ست — نه کاربر اپ موبایل که دنبال شگفتی بصریه. این یعنی خلاقیت باید در دقت و اعتمادسازی نشون داده بشه، نه در تجربه‌های غیرمنتظره. **الگوهای رد‌شده صریح:** انیمیشن پیچیده یا اسکرول غیراستاندارد، آیکون بدون برچسب متنی، و هر افکت گلس‌مورفیسم/گرادیانت پررنگ که تمرکز رو از محتوا می‌گیره.

**Key Characteristics:**
- تیره‌ی مطلق با یک لکه‌ی نارنجی، نه طیف رنگی
- بدون سایه در حالت استراحت؛ عمق فقط با لایه‌های سفید بسیار کم‌رنگ
- گوشه‌های نرم و پیوسته (rounded-lg تا rounded-2xl) روی زمینه‌ی کاملاً تیره
- تایپوگرافی تک‌خانواده (Vazirmatn) با تکیه بر وزن فونت برای سلسله‌مراتب، نه تغییر فونت

## Colors

پالت عمداً محدوده: یک زمینه‌ی تیره، یک رنگ سیگنال، و خاکستری‌های خنثی برای همه‌چیز دیگه.

### Primary
- **نارنجی سیگنال** (#f97316): تنها رنگ اقدام‌محور سایت — دکمه‌ی CTA اصلی، حالت فوکوس/انتخاب، لینک‌های برجسته، آیکون فعال. متن رویش باید تیره باشه (#060a17)، نه سفید — سفید رو این نارنجی فقط ۲.۸:۱ کنتراست می‌ده (زیر استاندارد WCAG AA)، رنگ تیره‌ی پس‌زمینه رویش ۷:۱ می‌ده.

### Secondary
- **آبی شناسایی** (#3b82f6 خانواده، معمولاً با شفافیت ۱۰-۳۰٪): فقط برای نشان‌گذاری/شناسایی به‌کار می‌ره — بج نقش کاربر، برچسب نوع فایل، تگ اعلان. هرگز به‌عنوان رنگ اقدام یا CTA استفاده نمی‌شه؛ نقشش صرفاً «این چیه» است، نه «این‌جا کلیک کن».

### Neutral
- **زمینه‌ی اتاق کنترل** (#060a17): پس‌زمینه‌ی اصلی کل سایت، تقریباً مشکی با ته‌رنگ سرمه‌ای.
- **سطح کارت** (#0f172a): پس‌زمینه‌ی پاپ‌اور/کارت‌های متمایز (مثل dropdown ها).
- **سفید عملیاتی** (#ffffff): متن اصلی روی زمینه‌ی تیره.
- **خاکستری خاموش** (#94a3b8): متن ثانویه/توضیحی (muted-foreground) — برچسب زیر مقدار، متن کمکی فرم.
- **حاشیه‌ی مویی** (rgba(255,255,255,0.10)): تنها ابزار جداسازی کارت/بخش از پس‌زمینه؛ نسخه‌ی کم‌رنگ‌تر rgba(255,255,255,0.05) برای جداکننده‌های داخلی سبک‌تر (مثل خط زیر نوار جست‌وجو).
- **لایه‌ی سطح** (rgba(255,255,255,0.03)): تنها راه بالا آوردن یک المان از پس‌زمینه بدون سایه — کارت‌ها همینو دارن، نه رنگ پرکننده‌ی جدا.

### Named Rules
**The One Signal Rule.** نارنجی سیگنال در هر صفحه فقط روی اقدام‌های واقعی (CTA، فوکوس، وضعیت فعال) ظاهر می‌شه؛ هرگز به‌عنوان رنگ تزئینی یا برای «قشنگ‌تر کردن» یک بخش استفاده نمی‌شه. اگه یک صفحه بیش از یک المان نارنجی هم‌زمان روی صفحه داره که رقابتی برای توجه ایجاد کنه، این قانون نقض شده.

## Typography

**Display/Body/Label Font:** Vazirmatn (Google Fonts, Persian/Arabic subset, `font-display: swap`) — تک‌خانواده برای همه‌چیز.

**Character:** خوانا، بی‌تجمل، بدون شخصیت تزئینی. سلسله‌مراتب فقط از طریق وزن فونت (۴۰۰ تا ۷۰۰) و اندازه ساخته می‌شه، نه تغییر فونت — دقیقاً مثل برچسب‌های روی یک دستگاه صنعتی: خوانایی مطلق، بدون افکت.

### Hierarchy
- **Display** (bold ۷۰۰، `text-3xl` تا `sm:text-5xl`، leading-tight، tracking-tight): تیتر هیرو، تنها یک بار در صفحه.
- **Headline** (bold ۷۰۰، `text-3xl` تا `lg:text-5xl`، leading-tight): تیتر بخش‌ها (مثل «دسته‌بندی محصولات»).
- **Body** (regular ۴۰۰، `text-sm`، leading حدود ۱.۶): متن توضیحی، فرم‌ها، محتوای کارت.
- **Label** (semibold ۶۰۰، `text-[11px]`، letter-spacing باز): برچسب‌های بالای گروه (مثل «جست‌وجوهای پرطرفدار»)، بج‌های نقش/وضعیت.

### Named Rules
**The Weight-Only Rule.** برای بالابردن اهمیت یک متن، وزن فونت رو زیاد کن یا اندازه رو بزرگ‌تر کن — هرگز فونت جدید اضافه نکن. یک خانواده‌ی فونت یعنی یک صدا.

## Layout

کانتینر اصلی حداکثر `max-w-5xl` تا `max-w-6xl` با پدینگ افقی ۱ تا ۲.۵rem بسته به breakpoint. فاصله‌گذاری بخش‌ها (`section-padding`) حدود `py-20` روی موبایل تا `py-28` روی دسکتاپ — بخش‌ها نفس می‌کشن، فشرده نیستن. کارت‌ها/گرید محصولات از `gap` (نه margin تکی) استفاده می‌کنن. موبایل-فرست: breakpoint های استاندارد Tailwind (`sm`, `lg`) با تغییر از تک‌ستونه به چندستونه در `lg`.

## Elevation & Depth

سیستم عمق مسطح است، نه لایه‌ای با سایه. در حالت استراحت هیچ کارتی سایه نداره — تمایز فقط با `bg-white/[0.03]` (لایه‌ی سفید بسیار کم‌رنگ روی پس‌زمینه‌ی تیره) و یک حاشیه‌ی مویی (`border-white/10`) ساخته می‌شه. سایه فقط به‌عنوان واکنش به تعامل ظاهر می‌شه (hover روی کارت، دکمه‌ی CTA اصلی) — نه به‌صورت پیش‌فرض.

### Shadow Vocabulary
- **hover-lift** (`shadow-lg` تا `shadow-xl`، رنگ سیاه یا نارنجی کم‌شفافیت): فقط روی hover کارت‌ها و دکمه‌ی CTA اصلی.
- **modal/dropdown** (`shadow-2xl`): برای عناصر شناور واقعی (پاپ‌اور، منوی موبایل) که باید از محتوای پشت‌شون جدا دیده بشن.

### Named Rules
**The Flat-At-Rest Rule.** سطوح در حالت عادی صاف‌اند؛ سایه فقط واکنش به state است (hover، فوکوس)، هرگز حالت پیش‌فرض یک المان.

## Shapes

گوشه‌ها نرم‌اند ولی نه گرد افراطی: `rounded-lg` (۰.۵rem) برای دکمه/ورودی، `rounded-2xl` (۱rem) برای کارت‌ها، `rounded-full` برای پیل‌ها/آواتارها/بج‌ها (پرکاربردترین شعاع در کل کدبیس). هدر ناوبری تنها استثناست با `rounded-3xl` — این تعمداً یک شکل خاص و متمایز برای عنصر ثابت بالای صفحه است. حاشیه‌ها همیشه مویی‌اند (`border-white/10` یا کم‌رنگ‌تر)، هرگز ضخیم یا رنگی به‌جز حالت فوکوس/خطا.

## Components

### Buttons
- **Shape:** `rounded-lg` (۰.۵rem)، ارتفاع پیش‌فرض ۲.5rem (h-10)
- **Primary:** پس‌زمینه‌ی نارنجی سیگنال، متن تیره (نه سفید) — طبق رفع کنتراست اخیر
- **Outline/Ghost:** بدون پرشدگی، فقط حاشیه یا hover هاله‌ای؛ برای اقدام‌های ثانویه
- **Hover / Focus:** رینگ فوکوس نارنجی (`focus-visible:ring-ring`)، تیره‌تر شدن پس‌زمینه روی hover (نه تغییر شکل)

### Chips / Badges
- **Style:** پس‌زمینه‌ی شفاف رنگی (نارنجی یا آبی شناسایی با ۱۰-۳۰٪ شفافیت)، متن هم‌رنگ در حالت پررنگ‌تر، `rounded-full`
- **State:** بدون حالت تعاملی — این‌ها فقط برچسب اطلاعاتی‌اند، نه دکمه

### Cards / Containers
- **Corner Style:** `rounded-2xl`
- **Background:** `bg-white/[0.03]`
- **Shadow Strategy:** بدون سایه در استراحت؛ `shadow-xl` روی hover (رجوع به Elevation)
- **Border:** `border-white/10` (گاهی transparent در حالت استراحت و رنگی روی hover)
- **Internal Padding:** معمولاً `p-6` تا `p-7`

### Inputs / Fields
- **Style:** `bg-white/5`، `border-white/10`، `rounded-lg`، پدینگ `px-4 py-3`
- **Focus:** حاشیه نارنجی کم‌رنگ + رینگ نارنجی شفاف (`focus:border-accent-500/50 focus:ring-2 focus:ring-accent-500/20`) — بدون تغییر پس‌زمینه یا سایه
- **Error:** پیام خطا مستقیماً زیر فیلد، با رنگ destructive

### Navigation
- کارت واحد شناور (`sticky top-0`) که نوار جست‌وجو و منو رو در یک شیء بصری واحد نگه می‌داره؛ گوشه‌ها همیشه نرم‌اند حتی در حالت چسبیده به بالای صفحه. روی دسکتاپ هنگام اسکرول، عمق (blur + shadow) اضافه می‌شه؛ روی موبایل این افکت غیرفعاله چون نوسان bounce آی‌او‌اس باعث چشمک‌زدن می‌شد.

## Do's and Don'ts

### Do:
- **Do** نارنجی سیگنال رو فقط برای اقدام واقعی به‌کار ببر (CTA، فوکوس، وضعیت فعال) — طبق The One Signal Rule.
- **Do** روی هر پس‌زمینه‌ی نارنجی از متن تیره (#060a17) استفاده کن، نه سفید.
- **Do** برای جداسازی/عمق از `border-white/10` + `bg-white/[0.03]` استفاده کن؛ سایه فقط روی تعامل.
- **Do** سلسله‌مراتب تایپوگرافی رو با وزن فونت و اندازه بساز، نه فونت جدید.
- **Do** برای مخاطب مسن‌تر/صنعتی، اقدام‌ها و برچسب‌ها رو همیشه با متن واضح همراه کن، نه فقط آیکون.

### Don't:
- **Don't** رنگ آبی شناسایی رو به‌عنوان دکمه یا CTA استفاده نکن — نقشش فقط بج/برچسب است.
- **Don't** سایه‌ی پیش‌فرض (بدون hover) به کارت‌ها اضافه نکن — این سیستم مسطح است، لایه‌ای نیست.
- **Don't** از گلس‌مورفیسم پررنگ، گرادیانت تزئینی سنگین، یا انیمیشن پیچیده/اسکرول غیراستاندارد استفاده نکن — مخاطب این سایت به سادگی و اعتماد نیاز داره، نه شگفتی بصری.
- **Don't** فونت دوم اضافه نکن؛ Vazirmatn تنها خانواده‌ی فونت سایت است.
